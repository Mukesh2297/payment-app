import { CreditFilterPipe } from './credit-filter.pipe';

describe('CreditFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
