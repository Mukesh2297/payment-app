import { TitleOnlyCasePipe } from './title-only-case.pipe';

describe('TitleOnlyCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleOnlyCasePipe();
    expect(pipe).toBeTruthy();
  });
});
